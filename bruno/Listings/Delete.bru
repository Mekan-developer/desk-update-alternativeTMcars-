meta {
  name: Удалить своё объявление
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/v1/listings/1
  body: none
}

headers {
  Accept: application/json
  Accept-Language: ru
  Authorization: Bearer {{token}}
}

docs {
  Только владелец (403 иначе). Удаляются и все медиафайлы объявления.
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
